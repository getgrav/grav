{#- Markdown output for AI agents and other text clients. Override per page type with `<template>.md.twig` in the theme. -#}
{%- autoescape false -%}
{{ markdown_output(page) }}
{%- endautoescape -%}
